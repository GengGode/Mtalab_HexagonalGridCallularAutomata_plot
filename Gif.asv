function Gif(gcf,pic_num)

%Gif 根据句柄抓取图像生成GIF
% gcf:绘图区句柄
% pic_num:GIF帧控制函数
F=getframe(gcf);
    I=frame2im(F);
    [I,map]=rgb2ind(I,256);
    if pic_num == 1
        imwrite(I,map,'test.gif','gif', 'Loopcount',inf,'DelayTime',0.2);
    else
        imwrite(I,map,'test.gif','gif','WriteMode','append','DelayTime',0.2); end
